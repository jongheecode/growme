---
name: project-design-asset-integration
description: "ABANDONED 2026-07-17 — html/ mockup to React-web port plan, superseded by the React Native pivot"
metadata: 
  node_type: memory
  type: project
  originSessionId: beeebcb0-7242-4d1b-927b-90d75b7c5d48
---

**Status: abandoned, do not resume.** Plan `docs/superpowers/plans/2026-07-16-design-asset-integration.md` ported 5 provided HTML mockups (login animation, home dashboard, history, profile, "꾸미" roaming-character playground) from the untracked `html/` folder into React DOM components. It was never started (no code referencing `html/` was written). On 2026-07-17 the user pivoted GrowMe to a React Native app with a different core loop — see [[project_v2_app_pivot]] — which makes this web-frontend porting plan moot: the web `frontend/` app itself is being abandoned, not just this one plan.

**How to apply:** Don't resume this plan. The `html/` mockups may still be useful as visual/interaction reference when building the equivalent RN screens, but treat them as inspiration only, not a porting target.
